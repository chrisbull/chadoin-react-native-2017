import { ApplicationStyles, TextColor } from '../theme'

const sharedNavigationOptions = {
  headerStyle: ApplicationStyles.navigationHeader,
  headerTintColor: TextColor,
}

export default sharedNavigationOptions
